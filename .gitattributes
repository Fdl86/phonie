# Dépôt portable : tous les fichiers texte sont stockés et extraits en LF.
* text=auto eol=lf

# Binaires.
*.ico -text
*.png -text
*.wav -text
*.bin -text
*.dll -text
*.exe -text

# Jeux de données vérifiés par SHA-256 : octets strictement inchangés.
data/radio/france/** -text
tests/Phonie.SmokeTests/Fixtures/radio/france/** -text
data/airports/**/*.json -text
